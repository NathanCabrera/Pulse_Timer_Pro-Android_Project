<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_ptp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_ptp_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PulseTimer">
        <activity
            android:name=".history.HistoryActivity"
            android:exported="false" />
        <activity
            android:name=".presets.PresetsActivity"
            android:exported="false" />
        <activity android:name=".history.SessionDetailActivity" />
        <activity
            android:name=".timer.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.PulseTimer">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>